services:
  - type: web
    name: financeflow
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python init_db.py && python migrate.py && uvicorn main:app --host 0.0.0.0 --port $PORT --proxy-headers --forwarded-allow-ips="*"

